# name: MasterDeployCI
# on:
#   push:
#     branches:
#       - master
# jobs:
#   build:
#     runs-on: ubuntu-latest
#     strategy:
#       matrix:
#         node-version: [10.x]
#     steps:
#     - uses: actions/checkout@v1
#     - name: Use Node.js ${{ matrix.node-version }}
#       uses: actions/setup-node@v1
#       with:
#         node-version: ${{ matrix.node-version }}
#     - name: Install Packages
#       run: npm install
#     - name: Deploy to GH Pages
#       run: |
#         git config --global user.email ${{secret.GITHUBEMAIL}}
#         git config --global user.name ${{secret.GITHUBUSERNAME}}
#         git remote set-url origin https://${{secret.MYACCESSTOKEN}}@github.com/${{secret.GITHUB_USERNAME}}/resty.git
#         npm run deploy
name: Deploy React App
on:
  push
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [12.x]
    steps:
    - uses: actions/checkout@v1
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: Deploy to GH Pages
      run: |
        git config --global user.name "${{ github.actor }}"
        git remote set-url origin https://${{ secrets.PERSONAL_TOKEN }}@github.com/${{ github.repository }}.git
        npm install
        npm run deploy